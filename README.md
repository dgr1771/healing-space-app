# 疗愈空间

一个专注于身心健康的健康管理与调养应用，包含凯格尔训练、睡眠监测和冥想练习三大核心功能。

## 功能特性

### 💪 凯格尔训练
- **三种难度级别**: 初级、中级、高级
- **智能计时器**: 自动引导收缩和放松
- **训练统计**: 追踪训练次数、时长、连续天数
- **历史记录**: 查看所有训练历史

### 🌿 身心疗愈
- **睡眠追踪**: 记录睡眠时长、质量和心情
- **冥想练习**: 多种冥想类型，包括呼吸练习、正念冥想等
- **睡眠监测**: 实时监测打鼾、梦话和黄金睡眠
- **健康总览**: 查看整体疗愈数据和健康建议

## 技术栈

- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite 5
- **样式**: Tailwind CSS 3（iOS风格设计）
- **路由**: React Router v6
- **状态管理**: Zustand
- **HTTP 客户端**: Axios

## 快速开始

### 安装依赖
\`\`\`bash
npm install
\`\`\`

### 启动开发服务器
\`\`\`bash
npm run dev
\`\`\`

应用将在 http://localhost:3001 启动

### 构建生产版本
\`\`\`bash
npm run build
\`\`\`

### 预览生产构建
\`\`\`bash
npm run preview
\`\`\`

## 项目结构

\`\`\`
man/
├── src/
│   ├── components/      # 通用组件
│   │   ├── common/     # 通用UI组件
│   │   └── layout/     # 布局组件
│   ├── modules/        # 功能模块
│   │   ├── kegel/      # 凯格尔训练模块
│   │   └── wellness/   # 身心疗愈模块
│   ├── pages/          # 页面组件
│   ├── services/       # API服务
│   ├── types/          # TypeScript类型定义
│   ├── stores/         # 状态管理
│   ├── App.tsx         # 应用根组件
│   ├── main.tsx        # 应用入口
│   └── index.css       # 全局样式
├── index.html          # HTML模板
├── package.json        # 项目配置
├── vite.config.ts      # Vite配置
├── tailwind.config.js  # Tailwind配置
└── tsconfig.json       # TypeScript配置
\`\`\`

## 开发说明

### 添加新功能
1. 在 `src/modules/` 下创建新模块文件夹
2. 在 `src/types/` 添加类型定义
3. 在 `src/services/` 添加API服务
4. 在 `App.tsx` 中添加路由

### 样式指南
- 使用 Tailwind CSS 工具类
- 主要颜色: `primary-500` (#0ea5e9)
- 卡片样式: `.card` 类
- 动画: `animate-fade-in`, `animate-slide-up`

## 后端API

应用需要后端API支持，默认运行在 http://localhost:8001

### API端点
- `/api/v1/kegel` - 凯格尔训练
- `/api/v1/wellness/sleep` - 睡眠记录
- `/api/v1/wellness/meditation` - 冥想记录
- `/api/v1/wellness/nutrition` - 饮食记录
- `/api/v1/wellness/stats` - 身心疗愈统计

## 许可证

MIT License
